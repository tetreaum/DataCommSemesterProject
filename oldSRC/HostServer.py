class HostServer:
        def __init__(self, serverPort, username):
            self.serverPort = serverPort  # instance variable unique to each instance
            self.username = username

       # def run(self):
